#!/usr/bin/env ruby

if ARGV.empty? or not Dir.exists? ARGV[0]
  ARGV.unshift Dir.pwd
end

repo = ARGV[0]
args = ARGV[1..-1] || []

# Use argv form to avoid invoking a shell with untrusted option strings.
exec("bugspots", repo, *args)
